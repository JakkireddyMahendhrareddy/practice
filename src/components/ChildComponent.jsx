// import React, { useContext } from "react";
// import { LoginContext } from "../App";

// const ChildComponent = () => {
//   const result = useContext(LoginContext);
//   return <div>{result ? "logged in" : "not logged in"}</div>;
// };

// export default ChildComponent;
